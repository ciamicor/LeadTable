const { Sequelize } = require( 'sequelize' );

module.exports = new Sequelize(
    "iami411_leadTable",
    "iami411",
    "+akWQ).ken1z", {
        host: "iami411.org",
        dialect: "mariadb",
    } )